html
  head
    title Mangareads
  body
    h1= reviews[0].Manga.title
    a(href=`reviews/add` value="post")
      button(type='button') Write a review
    each review in reviews
      p= review.user.firstName
      p= review.rating
