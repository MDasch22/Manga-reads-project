html
  head
    title Mangareads
    link(rel="stylesheet", href="/css/general.css")
  body
    table
      thead
        tr
          th
          th Title
          th Genre
      tbody
        each manga in mangas
          tr
            td= `${manga.title}`
            td= `${manga.genre}`
            td: a(href= `/mangas/${manga.id}`) More Info
